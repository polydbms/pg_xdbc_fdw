services:
  xdbc-server:
    image: xdbc-server
    restart: always
    volumes:
      - /dev/shm:/dev/shm
    ports:
      - 1234:1234
      - 1235:1235
      - 1236:1236
      - 1237:1237
      - 1238:1238
    shm_size: '16gb'
    cap_add:
      - NET_ADMIN
    labels:
      com.docker-tc.enabled: 1

  xdbc-client:
    image: xdbc-client
    restart: always
    volumes:
      - /dev/shm:/dev/shm
    shm_size: '16gb'
    cap_add:
      - NET_ADMIN
    labels:
      com.docker-tc.enabled: 1

  pg_xdbc_client_fdw:
    image: pg_xdbc_fdw_client
    restart: always
    volumes:
      - /dev/shm:/dev/shm
      - test-data:/data
      - pgvolume1:/var/lib/postgresql/
    shm_size: '16gb'
    cap_add:
      - NET_ADMIN
    labels:
      com.docker-tc.enabled: 1

volumes:
  test-data:
    external: true
  pgvolume1:

networks:
  default:
    name: pg-xdbc-net
